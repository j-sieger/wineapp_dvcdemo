from setuptools import setup,find_packages

setup(
    name="src",
    version = "0.0.1", 
    description="It is a wine Q package",
    author = "Jani Basha",
    packages = find_packages(),
    license = "MIT"
)
